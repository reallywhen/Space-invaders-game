/*Write a function that takes a string (example: “hello”)
 and returns the reversed version of the text (example: “olleh”)*/

var arr = [];

for (var i = 0; i < arr.length; i++) {
  arr[i] += 1;

  arr[0] = 0;
}

charArr[0] = "h";
charArr[1] = "e";
charArr[2] = "l";
charArr[3] = "l";
charArr[4] = "o";

var numberExample = [];

for (var i = 0; i < numberExample.length; i++) {
  numberExample[i] = numberExample[i] + 16;
}
